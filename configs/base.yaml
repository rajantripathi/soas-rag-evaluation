paths:
  raw_data_dir: data/raw
  processed_data_dir: data/processed
  eval_data_dir: data/eval
  index_dir: data/indexes
  results_dir: results
  logs_dir: results/logs
  corpus_file: corpus_smoke.jsonl
  eval_file: smoke_eval.jsonl
  index_name: smoke_index

dataset:
  smoke_max_docs: 120
  smoke_max_eval: 50
  generate_eval: true

retrieval:
  backend: simple_vector
  top_k: 3

generation:
  backend: stub

experiment:
  retrieval_mode: vector
  prompt_style: grounded
